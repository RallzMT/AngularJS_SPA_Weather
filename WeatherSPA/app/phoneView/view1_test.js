'use strict';

describe('myApp.phoneView module', function() {

  beforeEach(module('myApp.phoneView'));

  describe('phoneView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});